import { ProductModelUpdateDTO } from './product-model-update-dto';

describe('ProductModelUpdateDTO', () => {
  it('should create an instance', () => {
    expect(new ProductModelUpdateDTO()).toBeTruthy();
  });
});
