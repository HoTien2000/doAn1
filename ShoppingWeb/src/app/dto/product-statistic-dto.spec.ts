import { ProductStatisticDTO } from './product-statistic-dto';

describe('ProductStatisticDTO', () => {
  it('should create an instance', () => {
    expect(new ProductStatisticDTO()).toBeTruthy();
  });
});
