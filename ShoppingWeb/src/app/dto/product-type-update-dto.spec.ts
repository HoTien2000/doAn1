import { ProductTypeUpdateDTO } from './product-type-update-dto';

describe('ProductTypeUpdateDTO', () => {
  it('should create an instance', () => {
    expect(new ProductTypeUpdateDTO()).toBeTruthy();
  });
});
