import { PasswordResetToken } from './password-reset-token';

describe('PasswordResetToken', () => {
  it('should create an instance', () => {
    expect(new PasswordResetToken()).toBeTruthy();
  });
});
